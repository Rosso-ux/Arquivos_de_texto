# Leitor de Arquivo XML

Projeto Java que lê dados de um arquivo `.txt`, transforma cada linha em objetos Java e exibe as informações em formato XML.

Os dados são separados por `;` e convertidos automaticamente durante a leitura.
